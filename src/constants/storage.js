import { DEMO_ACCOUNT } from "./keys";

export const STORAGE = {
  requestToken: "requestToken",
  sessionId: "sessionId",
  credentials: "credentials",
  tempCredential: "tempCredentials",
};

export const USERS = {
  credentials: [],
  tempCredential: {},
};
